#!/bin/sh

. $(dirname $0)/system.conf


$qemu_system_arm -machine   vexpress-a9      \
                 -m         256M             \
                 -kernel    $uboot_image     \
                 -sd        $uboot_sd        \
                 -nographic
